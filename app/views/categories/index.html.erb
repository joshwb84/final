<% provide(:title, 'Library') %>
<h1> The library currently has <b><%= @inventory.count %></b> items in <%= @categories.count %> different categories</h1>
<div class="container">
  <!-- Example row of columns -->
  <div class="row">
    <h4>You can jump to a category or add a new item</h4>
    <div class="col-md-3">
        <div class="list-group">
        <% @categories.each do |category| %> 
        <a href="" class="list-group-item"><%= category.name %></a>
        <% end %>
      </div></div>
      <div class="col-md-3">
      <%= link_to "Add an item", new_item_path, class: "btn btn-success" %>
      </div>
    
  </div>
</div>

<% @categories.each do |category| %> 
<h3> <%= link_to category.name, category %></h3>
<table class="table table-striped table-hover ">
  <thead>
    <tr>
     <th>Available?</th>
      <th>Item Name</th>
      <th>Desc</th>
      <th>Storage spot</th>
      <th>identifier</th>
         <th></th>
          </tr>
  </thead>
<tbody>
  
  </tbody>
</table> 




<%end %>

