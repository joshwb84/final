<% provide(:title, 'Account page') %>

   

<h2><%= @user.name %></h2>
<h2><%= @user.playa_name %></h2>

<h2><%= @user.email %></h2>

<% @comments.each do |comment| %>
  <li><p>Regarding the <%= link_to comment.item.name, item_path(comment.item) %>, <%= @user.playa_name %> said "<%= comment.body %>"</p></li>
<% end %>

